# 완전 자동매매로 가는 개발·검증 기준

작성일: 2026-09-25 KST. 목표는 본인 키움 실계좌의 국내·미국 현물 주식 매수·매도·취소·체결 확인·장애 복구까지 사람의 일상 개입 없이 운영하는 것입니다. **수익은 구현 완료만으로 성립하지 않습니다.** 전략의 비용 차감 후 기대수익은 별도 전향 평가로 판단합니다.

현재 상태: 실계좌 읽기 전용 조회와 오프라인 안전 검사를 마쳤습니다. 실주문·체결 행·매도·취소의 실계좌 검증은 없고 자동 설정·arm도 없습니다. 실제 잔고와 설정 한도는 공개 문서 대신 로컬 원장에서 확인합니다. 2026-09-25은 국내 추석 휴장일입니다. [검증 기록](VALIDATION.md)을 참조하세요.

이번 단계에서 LIVE/DRY_RUN 손익과 모델 비용을 분리하고, 전일 기준 일 손실에 밤사이 가격 변동을 포함했으며, 실제로 모델을 호출하지 않은 주기는 호출 상한에서 제외했습니다. 이는 위험 계산의 정합성 수정이며 전략 수익성을 입증하지 않습니다.

연구 안전 단계 1 (2026-09-25, 코드·오프라인 테스트만, 실행·arm·실주문 없음):
- 증거 `stocklab-live-evidence-v2`: 제안자에게 가는 1분봉은 설정 캘린더의 현재 정규장 `[open_utc, close_utc]` 안의 것만 씁니다. 시가 이전 분봉은 버리고, 같은 세션 분봉이 5개 미만이면 그 주기는 관망합니다. 마감 이후 분봉·시가 이전 호가도 거부합니다. US는 시각 해석을 확정한 뒤 UTC로 비교합니다.
- 각 판단 행에 검증된 모델 스냅샷 전체(시장 데이터·`sellable`만, ≤ 64 KB)를 기존 해시와 함께 저장합니다. 스키마 변경 없음, v1 행은 재생 불가로 표시될 뿐 그대로 읽힙니다.
- 연구 전용 후보 `stocklab-research-costaware-drift-v1`: 같은 스냅샷·호가로 매 주기 계산해 제안·버전·입력 해시·비용 진단을 변경 불가 행에 기록합니다. 10개 1분 수익률의 t값(|t| ≥ 2)과 5분 단순 외삽이 왕복 비용 허들(스프레드·수수료·매도세·슬리피지·US 환전)을 넘을 때만 BUY/SELL 후보, 아니면 HOLD. 위험 엔진·주문·제안자 선택과 연결되지 않습니다. **수익성·보정 여부는 측정되지 않았습니다.** 창·기간·t 기준은 사전에 고른 값이며 튜닝되지 않았습니다. **이 t값은 신호 강도를 거르는 경험적 기준일 뿐 유효한 유의성 검정이 아닙니다.** 1분 수익률은 자기상관·변동성 군집·호가 반등 등 계열 의존성을 가지므로 독립 가정의 t분포나 p값을 적용할 수 없고, 10개 표본은 이 문제를 더 키웁니다. 5분 외삽도 실제 5분 뒤 가격과 대조해 보정한 적이 없는 단순 가정이므로 예상 수익률로 해석하면 안 됩니다.
- 오프라인 테스트: `tests/test_session_research.py`(KR/US 세션 경계, 스냅샷 저장·비공개 필드 없음, 비용 허들·관망, 연구 후보가 주문을 만들 수 없음).

모델 승격(연구 후보나 LLM을 LIVE 제안자로 바꾸는 일) 전에 아직 필요한 데이터:
- **과거 데이터**: 대상 종목의 여러 달 이상 정규장 1분봉과 같은 시점의 최우선 호가(스프레드), 거래정지·VI·상장폐지·기업행사·수정주가 이력, 공식 휴장·조기폐장 캘린더. 현재 저장소에는 이 이력이 없고, 키움 분봉 조회는 최근 구간만 줍니다. 시간순 학습/검증/미사용 구간과 사전 고정된 합격 기준이 필요합니다.
- **전향 데이터**: DRY_RUN 그림자 실행으로 쌓인 v2 판단 행(스냅샷·연구 후보·기준 규칙·제안자)을 최소 수십 거래일, 신호 시점 이후 실제 가격(5분 뒤 최우선 호가)과 짝지어 비용 차감 성과·HOLD 사유·체결 가능성을 집계해야 합니다. 이 집계·재생 도구는 아직 없습니다.
- **실제 비용**: 실계좌 체결 명세의 수수료·세금·환전 비용과 설정 bps 추정치의 대조. 소액 수동 왕복 주문(KR·US 각각) 전에는 허들 값 자체가 추정치입니다.

## 1. 데이터와 신호

- 현재 분봉 가격·거래량만 주는 [live_ai.py](stocklab/live_ai.py)의 LLM 제안, 단순 구간 수익률 기준선, 비용 허들을 둔 연구 전용 후보 [live_research.py](stocklab/live_research.py)를 연구 후보로 둡니다. 이 출력은 수익성이 검증된 예측이 아닙니다.
- 원천 데이터에 거래소·종목·통화·시각대·수정주가·상장폐지·기업행사·수집 지연을 기록합니다. 당시 알 수 있었던 자료만 각 판단 시점에 재생할 수 있어야 합니다.
- 먼저 거래비용이 낮은 단순 기준선(현금 보유, 단순 추세, 시장/섹터 지수 보유)을 고정합니다. 새 모델은 기준선과 동일한 종목·기간·비용·위험 한도에서 비교합니다.
- LLM은 분봉만 보고 매분 주문하는 주 엔진으로 쓰지 않습니다. 뉴스·공시처럼 텍스트의 시점과 출처를 확인할 수 있을 때 한정된 특징 추출 후보로 평가합니다. 실행 수량·가격·손절 규칙은 결정론적 위험 엔진이 정합니다.
- 신호 계약은 `BUY/SELL/HOLD`, 증거 ID, 모델 버전, 입력 데이터 시각, 예상 보유기간, 예상 비용 차감 수익과 불확실성을 분리해 기록합니다. 예상 수익이 비용과 불확실성 완충을 넘지 못하면 HOLD합니다.

## 2. 주문 상태 기계

- 국내 취소 `kt10003`, 미국 취소 `ust20003`은 공식 명세에 있지만 현재 코드에는 없습니다. 취소 자체를 새 주문 의도로 저장하고, 한 번만 전송하도록 클레임을 먼저 기록해야 합니다. 취소 응답은 체결·잔량 소멸의 증거가 아니므로 원주문과 취소 주문의 조회 결과를 모두 대사해야 합니다. [키움 API 가이드](https://openapi.kiwoom.com/guide/apiguide).
- 상태를 준비→전송시도→접수/결과불명→부분체결→취소시도→취소확인/전량체결/불확실로 구분합니다. 재시작 후 증권사 주문·체결·잔고를 다시 조회해 동일 결과를 중복 기록하지 않습니다. 어느 단계든 결과불명 상태에서는 재주문하지 않습니다.
- 미체결 지정가 주문은 정해진 시간 뒤 취소를 시도하고, **잔량 0이 조회로 확인될 때만** 새 주문으로 넘어갑니다. 취소와 체결이 동시에 일어나는 경주를 처리해야 합니다. 취소 확인이 불가능한 형식이면 신규 주문을 멈추고 운영자 알림을 냅니다.
- 매수·매도 모두 부분체결, 주문 거부, 정정·취소 자식 주문, 거래정지, 장 마감, 네트워크 중단, PC 재시작 시나리오를 다룹니다. 기존 `HUMAN_CLOSED`의 영구 차단을 검증된 취소·잔량 0·보유 일치 상태에서만 해제하는 감사 가능한 절차로 대체합니다.

## 3. 독립 위험 엔진

- 모델은 매매 방향 후보만 제안합니다. 수량은 실계좌 주문가능 현금, 보유 수량, 시장·종목·총 자본 한도, 스프레드, 예상 비용, 환율, 최소 거래단위 중 가장 작은 제한으로 계산합니다. 모든 비율 한도에 절대금액 상한을 함께 둡니다.
- 일 손실과 총 손실 규칙의 행동을 명시합니다. 신규 매수 중지와 보유 주식 청산은 별도 정책입니다. 청산을 선택한다면 신선한 호가, 거래 가능 수량, 가격 제한, 미체결 매도, 정지 상황을 같은 상태 기계로 처리합니다. 급격한 가격 변동에서는 설정한 손실 금액을 초과할 수 있습니다.
- 증권사 수수료·세금·환전·모델 호출 비용을 실제 체결 명세와 대조하고, DRY_RUN 비용과 LIVE 손익은 분리합니다. 모델 비용도 거래 가치 대비 상한을 둡니다.
- 미국 매수에는 주문 직전의 충분한 USD 주문가능액이 필요합니다. 환전은 별도 승인된 기능으로 구현·검증하기 전에는 자동화하지 않습니다.

## 4. 수익성 평가

- 모델·특징·종목 범위·매매 규칙·비용·합격 기준을 평가 전에 고정합니다. 시점 오류·생존 편향·데이터 누출을 검사하고, 시간순 학습/검증/미사용 평가 구간을 분리합니다. 튜닝한 설정을 같은 기간 성과로 합격시키지 않습니다.
- 비용 차감 수익, 현금 대비 초과수익, 최대 낙폭, 회전율, 체결률, 미체결률, 모델 비용, 시장 상태별 결과를 보고합니다. 1~2건의 실거래로 기대수익을 추정하지 않습니다.
- 실계좌 시세를 읽는 그림자 실행으로 여러 거래일 동안 신호와 가상 체결 가능성을 기록합니다. 장중 데이터 누락·휴장·시계 차이·종목 상태를 포함해 HOLD 이유를 집계합니다. 이후 작은 한도에서 실주문 왕복과 실제 비용을 검증합니다.
- [StockBench](https://arxiv.org/abs/2510.02209)는 다수 LLM 거래 에이전트가 단순 보유를 앞서기 어려웠다고 보고합니다. [FINSABER](https://arxiv.org/abs/2505.07078)는 좁은 평가에서 보인 우위가 더 넓은 기간·종목 범위에서 약해질 수 있음을 보였습니다. 이 결과는 이 저장소의 전략 성과를 대신 측정하지 않습니다.

## 5. 단계별 켜기 조건

1. 오프라인 재생에서 주문·취소·부분체결·재시작·중복 응답·결과불명·손실 규칙을 확인합니다.
2. 읽기 전용 실계좌 그림자 실행에서 장중 호가·분봉·거래일·보유 조회를 확인합니다.
3. 사용자가 정한 작은 절대 한도로 수동 매수→실제 체결·보유 대사→매도→대사를 완료합니다. 미국은 USD 주문가능액과 실제 미국 체결 행이 있어야 합니다.
4. 자동 실행은 한 시장·한 종목·일 최대 1건·짧은 arm부터 시작합니다. 모든 주문의 원장·증권사 상태가 일치해야 다음 주문으로 갑니다.
5. 운영 안전성과 비용 차감 성과를 별도로 평가합니다. 어느 한쪽이 부족하면 자동매매를 켜지 않습니다.

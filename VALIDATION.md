# 실계좌 검증 기록

확인일: 2026-09-25 KST. 이 문서는 **주문 전 단계까지의 검증**을 기록합니다. 실제 주문 전송, 접수, 체결, 취소, 매도, 수익성은 검증하지 않았습니다. 증권사 응답 원문, 계좌번호, 잔고액, 키, 주문번호는 저장하거나 이 문서에 적지 않았습니다.

## 직접 확인한 항목

| 범위 | 결과 | 한계 |
|---|---|---|
| 공식 실전 호스트 인증·국내/미국 조회 | `broker check --mode real --full` 성공 | 주문 API 호출 아님 |
| 국내 현금 `kt00001`, 미국 USD 현금 `ust21110`, 환율 `ust21160` | 필드 파싱 성공. 금액은 공개하지 않음 | 미국 매수에는 충분한 USD 주문가능액이 필요함. 자동 환전·원화 기반 미국 주문 없음 |
| 국내/미국 보유 `kt00018`/`ust21070` | 목록 형식 확인. 보유 현황은 공개하지 않음 | 체결 후 보유 수량·매매가능 수량의 동기화는 미확인 |
| 국내 주문내역 `kt00007` | 빈 목록 형식 확인 | 체결 행의 문구·시간·수량 형식 미확인 |
| 미국 주문내역 `ust21180` | 내역 없음에 해당하는 API 코드 20 확인 | 실제 주문 행과 페이지 목록 키 형식 미확인. 이 응답을 체결 대사 성공으로 간주하지 않음 |
| 국내/미국 분봉, 종목 상태 | 읽기 전용 응답 파싱 성공 | 국내 호가는 장외 시간에 `QUOTE_IN_FUTURE`로 거부됨. 정규장 신선도는 미확인 |
| 미국 호가, 종목 상태 | 읽기 전용 응답 파싱 성공 | 정규장 실주문 시점의 신선도·체결 가능성은 미확인 |
| 실전 원장·자동매매 상태 | `live status`, `auto status` 실행. 한도 값은 공개하지 않음. 자동 설정·arm 없음 | 한도 설정은 주문이 아니며, 실제 주문 전송 없음 |

프라이버시를 지킨 체크별 결과는 로컬의 `artifacts/live-readonly-validation.json`에 저장했습니다. 이 파일은 Git에서 제외됩니다. 재실행: `python -m tools.verify_real_readonly` (저장된 실전 키가 필요하며 읽기 전용 API만 사용).

## 오프라인 안전 검사

`python -m unittest discover -s tests -v`에서 39개 검사가 통과했습니다. 임시 `LOCALAPPDATA`와 가짜 전송기 및 메모리 데이터베이스를 사용했으며 실계좌 원장이나 주문 API를 사용하지 않았습니다. 확인한 항목은 한도 미설정 매수 차단, SQL 전송 근거·한도 검사, 티켓 불변성, 1회 전송권, 전송 직전 중지, 잘못된 주문 조건·접수 응답 거부, 동적 현금 예산, 사람 종결 후 차단, 주문번호의 날짜별 재사용(국내 KST·미국 동부 거래일), 미국 환율 교차 확인, 한도 변경 시 arm 무효화, 설정 없는 실시간 주기 차단, 미국 서머타임 경계, LIVE/DRY_RUN 손익·모델 비용 분리, 밤사이 가격 변동을 포함한 일 손실 기준, 첫 관측 손실, 모델 호출 상한에서 미호출 주기 제외입니다. `compileall`도 통과했습니다.

39개 중 19개는 `tests/test_session_research.py`의 연구 전용·세션·재생 검사입니다. 모두 합성 분봉·호가와 가짜 조회기를 쓰며 증권사·모델 호출은 없습니다.
- 세션 증거(8개): 국내 전일 분봉을 이어 붙이지 않고 버림, 같은 세션 분봉 5개 미만이면 주기 거부, 마감 이후 분봉 거부, 미국 KST/미 동부 시각 해석별 개장 경계 처리와 KST 자정을 세션 경계로 보지 않음, 유효한 세션 구간·현재 시각 포함 요구, 시가 이전 호가 거부.
- 스냅샷 저장·재생(2개): 판단 행에 비공개 필드 없는 전체 스냅샷 저장, 재생 읽기가 v2 행을 읽고 v1 행을 재생 불가로 허용.
- 연구 후보 계산(7개): 비용 허들 구성, 허들을 넘는 합성 증거의 BUY 후보, 비용이 우위보다 클 때 HOLD, 미국 환전 비용 포함, 부족·불규칙 증거에서 관망, 보유 상태별 규칙, 안전 래퍼가 예외를 내지 않음.
- 주문 차단(2개): 연구 후보를 제안자로 선택할 수 없고 주문·위험·키움 모듈을 참조하지 않음, 임시 원장·가짜 조회기로 돌린 오프라인 LIVE 모드 주기(티켓 준비·전송 함수는 호출 시 실패하도록 대체)에서 연구 후보가 BUY이고 기준 규칙이 HOLD일 때 위험 엔진에는 HOLD만 전달되고 티켓·의도·전송 클레임 행이 생기지 않으며 연구 기록은 `live_eligible=false`로 저장됨.

### LIVE 모델 제안자 OpenAI 전환 (2026-09-25)

LIVE `auto`의 `MODEL` 제안자를 Anthropic에서 OpenAI Responses API로 바꿨습니다(`provider`는 `openai`만 허용, Anthropic 대체 경로 없음). 과거 연구용 `run --strategy anthropic`은 그대로 두었고 LIVE 판단 경로가 아닙니다. `tests/test_live_openai.py`(12개)는 `urlopen`을 가짜로 바꾸고 소켓 연결을 차단한 상태에서 다음을 확인합니다: 정확한 요청 URL·헤더·본문(`tools: []`, `store: false`, `max_output_tokens`, strict `json_schema`), 유효한 BUY/SELL/HOLD와 감사 메타데이터(제공자, 요청·실제 모델, 응답 ID, 토큰 사용량, 프롬프트·스냅샷 해시, 키 미포함), 거절·`incomplete`/`failed`·오류 필드·여러 메시지/출력·도구 호출·잘못된 JSON·중복 키·범위 밖 제안·HTTP/네트워크/시간 초과/1 MB 초과가 모두 1회 시도 후 HOLD, 키 없음·`anthropic` 제공자·잘못된 모델 ID/토큰 한도에서 호출 없음, 설정이 `anthropic`·`claude*` 모델·이전 키 `max_tokens`를 거부, 템플릿 제공자가 `openai`이고 모델 기본값 없음. 이 변경 후 `python -m unittest discover -s tests -q`에서 61개 검사가 통과했습니다. 실제 OpenAI·키움 호출은 하지 않았고, 실제 OpenAI 응답 형식과 모델별 추론 토큰 소비는 실호출로 확인하지 않았습니다. 구현은 Claude Code **claude-opus-5-5**가 Codex 감독 아래 소스·문서·오프라인 검사만 수정해 수행했습니다.

### LIVE 모델 제안자 Codex CLI(ChatGPT 구독) 전환 (2026-09-25)

`MODEL` 제안자를 OpenAI Responses API(키·API 과금)에서 Codex CLI `codex exec`(저장된 ChatGPT 로그인)로 바꿨습니다. `provider`는 `codex_cli`만 허용하고 설정 필드는 `model`·`max_calls_per_day`·`timeout_seconds`입니다. 이전 `openai` 설정·토큰 단가 필드는 거부되며, 구독 호출 비용은 0원으로 기록하고 이전 API 호출 행의 비용은 그대로 합산됩니다. `tests/test_live_openai.py`는 `tests/test_live_codex_cli.py`로 대체합니다. 이 파일은 `live_ai._run`을 가짜로 바꾸고 소켓 연결을 차단해 다음을 확인합니다: 로그인 확인 후 정확한 `codex exec` 인자·빈 임시 작업 디렉터리·표준 입력 프롬프트·출력 스키마, 환경변수에서 키움·API 키 제거, 구독 감사 메타데이터(제공자·모델·사용량·0원), API 키 로그인·미로그인·CLI 없음·사용량 제한·시간 초과·크기 초과·도구 활동·종료 코드·이벤트/출력 파일 오류·잘못된 제안이 모두 1회 시도 후 HOLD이며 stderr 원문 미기록, 잘못된 모델 ID·시간 한도·제공자에서 실행 없음, 소스에 HTTP/API 경로 없음, 설정·템플릿 검증, 설정 없음·DRY_RUN·LIVE(로그인 실패) 주기에서 주문 없음, 이전 `openai` 설정은 주기를 멈추고 새 설정 저장 시 arm 무효. 제한 실행기는 짧은 로컬 Python 자식 프로세스로 시간 초과·출력 한도·도구 활동 즉시 종료를 확인합니다. **실행 결과: Codex가 `python -m unittest discover -s tests -q`로 오프라인 검사 73개 통과를 확인했습니다.** Claude Code는 실행 권한 제한으로 검사를 직접 실행하지 못했습니다. 설치된 CLI의 `codex login status`는 ChatGPT 로그인을 보고했습니다. Codex가 가짜 시세로 실제 `gpt-6-sol` 구독 호출을 두 번 실행했고, 두 번 모두 CLI JSONL·스키마·ChatGPT 인증 경로가 통과했습니다. 같은 입력에서 한 번은 BUY, 한 번은 SELL 제안이 나와 판단 일관성·수익성은 입증되지 않았습니다. 실제 장중 시세·Windows npm 셰임 환경·실계좌 주문은 확인하지 않았습니다. 키움 호출·주문·arm은 하지 않았습니다. 기본 구현은 Claude Code **claude-opus-5-5**가 수행했고, Codex가 셸·웹 도구 차단 설정을 추가하고 검증했습니다.

### 시계열 모델 + Codex 2단계 경로 (2026-09-25)

`stocklab/ts_forecast.py`(의존성 없는 ARX/ridge 예측기·불변 산출물·엄격한 로더·학습 CLI), `stocklab/hybrid_eval.py`(과거 2단계 오프라인 검증 CLI)를 추가하고, LIVE `MODEL` 주기가 Codex 호출 전에 고정된 산출물로 예측을 계산하도록 연결했습니다. 예측이 없거나 산출물 해시·기한·분봉 조건이 어긋나면 Codex를 부르지 않고 HOLD하며, Codex 제안 뒤에는 예측 순수익 ≤ 0인 BUY와 예측 총수익 ≥ 0인 SELL을 HOLD로 바꾸는 게이트가 있습니다. `forecast` 블록이 없는 기존 `MODEL` 설정은 이제 매 주기 HOLD입니다. `BASELINE`, 위험 엔진, 한도, arm, 주문 경로는 바꾸지 않았고 `cycle.lookback_bars` 상한만 30에서 31로 늘렸습니다.

추가·수정한 오프라인 검사: `tests/test_ts_forecast.py`(특징·목표 정렬, t 이후 값 무시, 분봉 끊김·세션 경계 거부, 학습 세션 밖 데이터 변경에 대한 불변성, 고정 적격 기준, 결정적 학습, 학습 CLI의 필수 기준일·덮어쓰기 거부·공개 경로 거부, 산출물 해시 고정·내용 해시·필드·버전·NaN·중복 키·불리언·길이·출처·종목 변조 거부, 예측 객체 검증·프롬프트 병합·게이트 통과/거부·잘못된 예측에서 호출 없음, 가짜 조회기·가짜 Codex 실행기로 돌린 DRY_RUN/LIVE 주기의 예측 전달·감사 기록·HOLD 경로·주문 없음), `tests/test_hybrid_eval.py`(학습/시험 분리, 가격과 무관한 판단 일정, 거래 회계·복리, Codex 없을 때 호출 없음, 익명화 입력·호출 상한·오류 후 중단, t 이후 값 미사용, CLI 플래그 검증, 주문·브로커 모듈 미로드), `tests/test_live_codex_cli.py`의 템플릿·예측 미설정 주기 기대값 갱신. Claude Code(claude-opus-5-5)는 이 세션에서 검사 명령 권한이 거부되어 직접 실행하지 못했습니다. **Codex가 오프라인 검사 104개 통과를 확인했습니다.**

실제 키움 읽기 전용 분봉 CSV로 기본 비용 가정의 마지막 5세션을 분리해 시험했습니다. 삼성전자(KR 005930)는 학습 20세션·시험 5세션·판단점 55개, AAPL(US)는 학습 15세션·시험 5세션·판단점 55개입니다. 각 예측의 30분 방향 적중률은 KR 43.4%, US 52.73%였고 평균절대오차는 각각 31.20bp, 15.87bp였습니다. 예시 왕복 비용 53bp(KR), 100bp(US)를 초과하는 예측이 없어 모델 단독 매수 0건이며, **이 조건에서 Codex 매매판단 호출 0건·하이브리드 거래 0건**입니다. 수익률 0%는 거래를 하지 않았다는 뜻이지 성과 입증이 아닙니다. 보고서는 `artifacts/hybrid-kr-model-only.json`, `artifacts/hybrid-us-model-only.json`에만 저장했습니다.

연결 경로만 확인하려고 거의 0bp인 비현실적 비용(스프레드 0.01bp, 나머지 0bp)으로 시장별 Codex `gpt-6-sol` 호출을 2회씩 수행했습니다. 네 호출 모두 오류 없이 완료됐고 하이브리드 매수는 0건이었습니다. **이 실험의 수익률은 실제 거래비용을 반영하지 않으며 실전 성과로 해석할 수 없습니다.** 보고서는 `artifacts/hybrid-kr-codex-near-zero-cost-check.json`, `artifacts/hybrid-us-codex-near-zero-cost-check.json`에만 저장했습니다. 실제 키움 호출·계좌 조회·arm·주문은 하지 않았습니다. 라이브 분봉의 완결 여부와 실제 수수료·세금·스프레드·슬리피지는 검증되지 않았습니다.

이 검사는 코드 경로의 동작만 확인합니다. 실제 장중 데이터로 실행하지 않았고, 연구 후보의 예측력·보정·수익성, 실계좌 주문·체결은 검증하지 않았습니다.

일 손실 기준의 이전 기록은 프로그램이 저장한 마지막 관측값입니다. 장중 기록이거나 며칠 전 기록일 수 있으므로 공식 전일 종가와 일치한다고 가정할 수 없습니다. 실시간 시세의 누락과 실제 체결 비용은 여전히 별도로 검증해야 합니다.

독립 정적 검토에는 Claude Code **claude-opus-5-5**를 읽기 전용 계획 모드로 사용했습니다. 검토 범위는 주문·대사·자동 실행 코드, 공식 SDK/명세, 문서였습니다. 키·계좌 응답·원장을 제공하지 않았고 Claude는 프로그램이나 증권사 호출을 실행하지 않았습니다. Opus가 지적한 날짜가 다른 주문번호 재사용 차단 결함과 수동 미국 매수의 환율 교차 확인 누락은 Codex가 수정하고 각각 오프라인 검사로 확인했습니다. 검토 실행 메타데이터는 Git에서 제외한 로컬 `.reviews/`에 보관합니다.

후속 손익 계산 검토에도 Claude Code **claude-opus-5-5**를 사용했습니다. 이번 구현 실행에서는 편집 권한이 거부되어 Claude가 파일을 바꾸지 못했습니다. Codex가 LIVE/DRY_RUN 분리, 전일 기준 일 손실, 모델 호출 집계 수정을 적용하고 위 오프라인 검사를 실행했습니다. 실행 메타데이터는 Git에서 제외한 로컬 `.reviews/`에 보관합니다.

## 실제 주문 검증의 남은 조건

1. 계좌의 주문가능액을 조회하고 한도를 로컬 원장에 기록했습니다. 실제 잔고와 설정 값은 공개하지 않습니다. `auto config`·arm은 여전히 없습니다.
2. 2026-09-25 KST는 추석 휴장일입니다([정부의 9월 24~27일 연휴 안내](https://www.mois.go.kr/frt/bbs/type010/commonSelectBoardArticle.do?bbsId=BBSMSTR_000000000008&nttId=129490), [한국거래소 휴장 규칙](https://global.krx.co.kr/contents/GLB/06/0602/0602010201/GLB0602010201T1.jsp)). 국내 장중 호가 신선도와 종목 상태를 다음 개장일에 다시 확인해야 합니다. 미국 정규장도 같은 검사가 필요합니다.
3. 특정 종목·방향·수량·지정가·예상 비용·한도 영향을 검토한 뒤 첫 소액 주문을 전송하고 증권사 앱과 `live reconcile`의 결과를 대조해야 합니다. 실제 주문번호/체결 행이 없는 현재는 대사 파서의 운영 적합성을 입증할 수 없습니다.
   - 기술 검증 중 조회한 종목과 가격은 공개하지 않습니다. 다음 개장일에 신선한 최우선 매도호가, 스프레드, 거래 상태와 잔고를 다시 확인하고 설정 한도를 넘으면 주문하지 않습니다.
4. 미국 매수를 시험하려면 먼저 충분한 USD 주문가능액이 필요합니다. 환전은 현재 프로그램 범위 밖입니다.
5. `HUMAN_CLOSED`로 미체결 또는 부분체결 티켓을 종결하면 자동매매가 영구 차단되는 보수적 정책입니다. 이 상태의 자동 복구 절차는 없습니다. 첫 주문 전에 운영 절차를 결정해야 합니다.

이 검증은 전략의 기대수익이나 손실 한도를 입증하지 않습니다. 한도·손실 기준 코드는 가격 급변, 체결 지연, 환율 및 수수료 차이로 실제 손실을 제한한다고 보장할 수 없습니다.

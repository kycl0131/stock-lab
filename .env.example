# Optional, explicit model inference only. The application never loads this file.
# Set these in your shell or secret manager; never commit real values.
# LIVE `auto` MODEL proposer (OpenAI Responses API only; model ID comes from the auto config):
OPENAI_API_KEY=
# Historical research CLI only (`run --strategy anthropic`); not used by the live decision path:
ANTHROPIC_API_KEY=
STOCKLAB_MODEL=

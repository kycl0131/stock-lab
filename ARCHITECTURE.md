# 구조 결정

작성일: 2026-09-25. 대상: 본인 명의 키움 계좌, 국내·미국 현물주식. 초기 계획(국내 50,000원·미국 50,000원 배정)은 사용자가 철회했습니다. 실전 매수 한도는 `live cap`으로 명시한 값만 사용하며, 설정 전에는 매수가 차단됩니다. 아래의 50,000원 표현은 로컬 모의 시범과 `pilot` 계산기에만 해당합니다.

**현재 구현·검증 현황은 [VALIDATION.md](VALIDATION.md)와 README를 기준으로 읽으세요.** 아래의 단계별 계획 및 과거 충족 현황은 작성 당시의 기록입니다. 현재 코드에는 국내·미국 BUY/SELL, 양 시장 주문내역 대사, 한도·arm으로 차단되는 무인 실행 경로가 있습니다. 다만 실제 주문·체결 대사는 검증하지 않았고 자동매매를 활성화하지 않았습니다.

## 결정

연구 계획(`../research-debate/research-informed-plan.md`)이 **시스템 계약**을 정하고, GitHub 구성요소는 그 계약 안의 교체 가능한 부품으로 둡니다. 기존 프레임워크 하나를 골라 그 위에 실전 매매를 얹지 않습니다.

| 계층 | 담당 | 비고 |
|---|---|---|
| 시점 통제 평가, 실험 장부, 결정 해시 | Stock Lab | `available_at`/`ingested_at` 필터, 모든 실행·실패 기록 |
| 독립 위험 검사, 중복 주문 방지, 비용 계산 | Stock Lab | 모델 출력과 분리된 코드로 검사 |
| 브로커 인증·통신 | 키움 공식 SDK | 별도 로컬 의존성(`vendor/`, Git 제외). 라이선스상 코드 복사·수정·배포 금지이므로 import만 하고 수정하지 않음 |
| 수치 모델 | Qlib (후보) | 결과를 목표 비중 JSON 계약으로 가져오는 방식. **어댑터 없음** |
| 뉴스/토론형 AI | TradingAgents (후보) | 동일 계약·동일 자료 묶음으로 비교할 후보. **어댑터 없음**, 주문 실행 경로로 쓰지 않음 |

Qlib·TradingAgents는 모델·평가 후보일 뿐 브로커 실행 시스템이 아닙니다. 이들의 출력은 항상 Stock Lab의 검증·위험 검사를 거치며, 증권사와 직접 연결하지 않습니다.

## 현재 경계

- 키움 조회: `KiwoomReadOnly`(`kiwoom_bridge.py`)는 계속 조회 API 허용 목록만 호출합니다(이번에 `ust21160` 예수금 상세 조회만 추가). `serve-real`은 실전 계좌 **전체**의 실제 조회값을 로컬에서 표시하며 주문 경로가 없습니다.
- 키움 주문: 쓰기 기능은 별도 모듈 `kiwoom_order.py`의 `KiwoomRealOrderClient`에만 있습니다. 고정된 4개 API(`kt10000`/`kt10001`, `ust20000`/`ust20001`)로 지정가 주문만 만들고, 실전 호스트를 고정하며, SDK 인증 재시도를 끄고 1회만 호출합니다. 호출 경로는 `live_orders.py`의 티켓 워크플로(`live send`, 확인 문구 직접 입력)와 유효한 arm이 필요한 `live_auto.py`뿐입니다. `submit`은 원장에서 방금 `ATTEMPTED`로 기록된 티켓·전송 근거를 다시 확인한 뒤에만 전송합니다. 정정·취소·환전·시장가·신용 주문 API는 없습니다. `serve-real`과 `broker` 명령은 이 모듈을 import하지 않습니다.
- 실전 주문 DB: 사용자 프로필 `%LOCALAPPDATA%\StockLab\live-orders.db`의 전용 파일입니다. 티켓은 삭제·조건 변경이 불가하고 상태는 한 방향으로만 바뀝니다. 명시 한도(`risk_caps`)·현금비율·중지·미종결 주문·만료·이 프로그램에서 확인된 시범 보유 수량(SELL)은 코드와 트리거에서 이중으로 검사합니다. 체결(`fills`, `fills_us`)·주문 식별(`order_links`)·종결(`resolutions`)은 추가만 가능합니다. 대사는 국내 `kt00007`, 미국 `ust21180` 읽기 전용 조회를 사용하지만 실제 체결 행 형식은 아직 미검증입니다. 다른 작업 폴더에서도 같은 DB를 사용하지만 파일 삭제·다른 PC의 주문까지 자동 대사할 수는 없습니다. 자세한 규칙과 남은 작업은 README의 "실전 주문 티켓".
- 주문: 로컬 모의 체결(`run --mode paper`, `advance`)만 있습니다. 실전 잔고와 동기화하지 않습니다.
- `pilot`: 사용자가 입력한 단가·수량·수수료·매도 세율·슬리피지·환율로 비용과 손실 시나리오를 계산하는 오프라인 보고서입니다. 시장별 `--budget-krw 50000`을 따로 계산합니다. DB·네트워크·키 저장소·증권사를 사용하지 않고 종목을 추천하지 않습니다.
- `pilot-setup`/`pilot-status`/`pilot-demo` (`stocklab/paper_pilot.py`): 로컬 두 시장 모의 시범. 아래 "로컬 모의 시범" 참고.

## 세 환경의 경계

| 환경 | Stock Lab 구현 범위 |
|---|---|
| 로컬 모의 시범 (SQLite) | 가상 주문 생성·가상 체결·평가·손실 기준 통제 전부. 증권사 호출 없음 |
| 키움 공식 모의투자 서버 | `broker --mode demo` 인증·조회만. 모의 주문 전송·체결 조회·대사는 **미구현**. 사용자가 발급한 별도 모의 키 필요 |
| 키움 실전 계좌 | `broker --mode real`과 `serve-real` 조회. `live`: 사람 확인형 지정가 주문. `auto`: 명시 설정·arm 필요, 기본 비활성. 국내·미국 대사·시범 보유분 SELL과 손실 기준 차단 코드가 있으나 실제 주문·체결 검증은 미완료 |

로컬 장부는 어느 증권사 잔고와도 동기화하지 않으며, 증권사 조회 결과를 로컬 모의 장부 평가에 쓰지 않습니다. `serve-real`은 로컬 장부를 사용하지 않으며 계획상 5만 원씩의 시범 자금이 실제 계좌에 따로 배정됐다고 가정하지 않습니다.

## 로컬 모의 시범

- **생성**: `pilot-setup`은 존재하지 않거나 0바이트인 DB 경로에서만 실행됩니다(파일을 열기 전에 검사, 트랜잭션 안에서 빈 DB인지 재검사). 입력 검증은 DB 파일 생성 전에 끝냅니다. 두 계좌(KRW 50,000, 달러 환산액)와 시범 기록을 한 `BEGIN IMMEDIATE` 트랜잭션으로 만듭니다.
- **기록**: `settings.paper_pilot`에 시장별 한도·손실 기준, 환율·환전 비용, 환산·반올림 규칙을 저장합니다. 기준 도달은 `settings.paper_pilot_breach:{KR|US|COMBINED}`에 기록합니다. `paper_pilot*` 키는 트리거로 수정·삭제가 금지됩니다. 즉 도달 상태는 그 DB에서 해제되지 않습니다.
- **환산**: 미국 현금 = `floor_to_cent(50,000 / (rate × (1 + cost)))`. 원화 평가 = USD × rate × (1 − cost). 환전 비용이 설정 시와 평가 시 모두 부과되므로 시작 직후 미국 계획 손실은 0보다 큽니다(왕복 환전 비용 + 잔여 원화).
- **평가**: 비용 가정 반영 평가액 = 현금 + 보유 평가액 × (1 − (fee_bps + slippage_bps)/10000). 시세는 평가 시점 이하(`event_at`, `available_at` 모두)이고 RiskPolicy의 유효기간 이내여야 합니다. 시범 DB의 유효기간은 최대 24시간이며 거래 캘린더가 없어 미국 종목 보유 중 월요일 국내 신규 매수는 시세 만료로 차단됩니다. 평가 시점이 해당 계좌 시계보다 이르면 거부합니다. 합계는 두 장부를 같은 시점에 평가하므로 다른 시장 시계가 앞서 있으면 평가 불가(= 신규 매수 차단)입니다.
- **통제 시점**: (1) `run --mode paper` 주문 생성 시 매수마다 한도·기준·예상 손실 검사, 불가하면 `REJECTED`. (2) `advance`의 모든 시세 관측 시점과 체결 직후 기준 도달을 확인해 고정하고 미체결 매수를 취소합니다. 매수 체결 직전에도 같은 검사; 기준 관련이면 주문 `CANCELLED`, 평가 불가면 체결 건너뜀(`FILL_SKIPPED`). (3) `advance` 종료 시 재검사. 읽기 전용 `pilot-status`는 기준 도달을 기록하지 않습니다. 매도는 별도 계획 손실 기준에 막히지 않지만 유효한 시세와 일반 엔진 검사는 필요합니다. 자동 청산은 없습니다.
- **기존 동작 보존**: `paper_pilot` 기록이 없는 DB는 위 검사를 전혀 거치지 않습니다. 실행 키 멱등성, 같은 관측 시점 체결 금지, 원자적 장부 갱신은 그대로입니다. `advance`는 루프 중 취소된 주문을 다시 읽어 건너뛰도록 한 줄 추가했습니다(기존 DB에서는 결과 동일).
- **알려진 한계**: 고정 환율, 기업행사·세금·최소 수수료·규제 수수료 미반영, 유동성·호가·거래정지 미반영, 거래 캘린더 없음. 사용자 수수료·슬리피지 가정이 실제보다 작으면 손실을 과소평가합니다. 기준은 보장된 손절이 아닙니다.

## 시범 자금과 운영 시간

- 국내와 미국에 각 50,000원을 배정합니다. 한 시장의 잔액을 다른 시장에 자동으로 옮기거나 재사용하지 않습니다.
- 계획 손실 기준은 시장별 2,500원, 합계 5,000원입니다. 도달하면 신규 매수를 멈추는 것이 향후 실주문 기능의 필수 조건입니다. 현재는 로컬 모의 시범(`pilot-setup` DB)에서만 집행됩니다. 급변동·환율·체결 공백 때문에 실제 손실을 이 금액으로 보장할 수는 없습니다. 자동 청산은 구현하지 않습니다.
- 두 시장을 매일 거래하는 일정은 세우지 않습니다. [한국거래소 정규장](https://global.krx.co.kr/contents/GLB/06/0602/0602010201/GLB0602010201T1.jsp)은 한국시간 09:00~15:30, [미국 정규장](https://www.nyse.com/markets/hours-calendars)은 미국 동부시간 09:30~16:00입니다(서머타임 기간은 한국시간 22:30~익일 05:00). [키움의 미국 주간거래](https://www.kiwoom.com/e/m/home/event/VEvent20250160View)는 별도 세션이고 정규장보다 호가가 넓거나 체결이 제한될 수 있습니다. REST API에서 자동주문을 지원하는지와 체결 품질을 검증하기 전에는 시범 자동주문 대상으로 간주하지 않습니다.

## 국내 50,000원·미국 50,000원 시범이 확인할 수 있는 것과 없는 것

확인할 수 있는 것:
- 실제 계좌의 수수료·환전·최소 수수료가 예상치와 얼마나 다른지(체결 후 명세와 `pilot` 계산의 차이).
- 주문 입력·체결·잔고 반영·결제 과정과 기록 절차가 의도대로 작동하는지.
- 작은 금액에서 비용이 손익분기에 미치는 크기(소액일수록 정액·최소 수수료 비중이 커짐).

확인할 수 없는 것:
- 전략이나 AI 모델의 수익성. 몇 건의 거래는 통계적 근거가 되지 않습니다.
- 큰 금액에서의 시장충격, 호가 대기열, 유동성.
- 장기 위험, 최대 낙폭, 세금 영향의 전체 모습.

좋은 시범 결과를 근거로 금액을 늘리지 않습니다. 확대는 연구 계획의 전향 검증(유망/근거 부족/불리한 근거 구분)과 별도의 결정입니다.

## 실전 주문 구현 전 필수 조건 (초기 설계 기록)

아래 충족 현황은 당시 기록이며 현재 코드 상태가 아닙니다. 최신 현황은 [VALIDATION.md](VALIDATION.md)를 보세요.

2026-09-25 사용자 결정으로 첫 실전 주문 단계(`live`)를 추가했습니다. 충족 현황: 2(주문별 수동 승인)와 7(조회·주문 경로 분리)은 구현, 1(한도)은 처음에 시장별 누적 예약 50,000원 고정 상수로 구현했고, 이후 `live cap` 명시 한도(누적·주문당·주문가능현금 비율, 미설정 시 매수 차단)로 교체, 3(사전 검사)은 금액·정수·국내 주권 호가단위·주문가능 현금만 구현(운영시간·가격제한폭·거래정지 없음), 4(멱등성)는 재전송 금지·결과불명 차단과 국내 `kt00007` 대사(전량 체결 자동 종결, 그 밖은 사람 종결)까지 구현하고 **US 대사는 미구현**, 5(중지)는 영구 `halt`만 있고 **손실 기준 집행은 미구현**, 6·8(비용 확정·체결 기록)은 미구현, 9(환전은 사용자가 직접)는 유지. 미구현 항목 때문에 US SELL과 무인 자동주문은 차단하고, SELL은 국내 시범 보유 수량으로만 허용합니다.

아래 항목은 무인·AI 자동주문 전 필수 조건입니다.

1. **명시적 사용자 승인 범위**: 시장, 종목 범위, 건당·일간·총 금액 상한, 손실 한도를 사용자가 문서로 정하고 코드 상수가 아닌 검증된 설정으로 적용.
2. **주문별 수동 승인**: 초기에는 모든 실전 주문을 사람이 확인한 뒤 전송. 모델이 직접 주문하지 않음.
3. **독립 사전 위험 검사**: 금액·수량 정수·호가단위·가격제한폭·거래정지·시장 운영시간·주문 가능 금액 검사를 모델과 분리된 코드에서 수행.
4. **멱등성과 대사**: 고유 주문 키, 응답 불명확 시 재전송 금지 후 체결·미체결 조회로 상태 확정, 재시작 후 실전 잔고와 로컬 장부 대사.
5. **중지 장치**: 전역 중지 스위치, 시장별 2,500원·합계 5,000원의 계획 손실 기준 도달 시 신규 주문 차단, 자동 청산은 별도 승인. 실현·미실현 손익과 수수료·환전 비용을 일관된 시점에 집계.
6. **비용 확정**: 실제 계좌의 수수료·세금·환전 조건을 체결 명세로 확인하고 비용 모형에 반영.
7. **환경 분리와 검증**: 실전 키를 별도 보관하고 조회·주문 경로를 분리. 사용자가 모의 환경 작업을 중단했으므로 실전 주문 전 검증 방법과 잔여 위험을 별도로 확정해야 함.
8. **기록**: 주문 요청·응답·체결을 원본 시각과 함께 보존하되 키·토큰·계좌번호는 기록하지 않음.
9. **환전 API는 별도 범위**: 미국 주식 시범에서도 환전은 사용자가 직접 수행하고 프로그램은 입력 환율만 사용.
